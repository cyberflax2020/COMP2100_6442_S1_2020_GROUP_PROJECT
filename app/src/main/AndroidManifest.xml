<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication111">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Search_result"></activity>
        <activity android:name=".Surveys_published" />
        <activity android:name=".Vote" />
        <activity android:name=".Survey_view" />
        <activity android:name=".Survey_result" />
        <activity android:name=".Survey_engage" />
        <activity android:name=".Question_type" />
        <activity android:name=".Logout" />
        <activity android:name=".SendPage" />
        <activity android:name=".Edit_questions" />
        <activity android:name=".Creating_title" />
        <activity android:name=".Home_page" />
        <activity android:name=".Register" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>